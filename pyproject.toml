[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "hello"
version = "0.0.1"
description = "Small example package for GitHub Actions demo"
authors = [
  { name = "Your Name" }
]
readme = "README.md"
requires-python = ">=3.8"
